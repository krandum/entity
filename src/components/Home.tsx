import * as React from 'react';

export default class Home extends React.Component<any, any> {
    render() {
        return (
            <div id="home">
                <h1>
                    Home
                </h1>
                This will be the home page for the entity system, and will show some of its basic features as well as eventually {
                }instruct the user to login and create a character.
            </div>
        );
    }
}